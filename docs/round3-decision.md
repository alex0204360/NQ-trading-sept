# Round 3 decision, before scoring

Rounds 1 and 2 produced no eligible family or surviving signature. Round 3 therefore uses the fallback definitions already committed in the candidate generator before either market round was scored: price extremes with wick rejection, and reversal-shaped candles near prior swing extremes.

There are sixteen signatures in each approach. They refine distinct price-state and swing-geometry families; none is described as a refinement of a winner. This fixes the implementation choice left open by the Phase 0 fallback wording. All 32 exact definitions are in this round's published manifest. No threshold, holding window, baseline, support requirement, inference budget, or stopping rule changes.

The quality score is currently zero. If this round also improves it by less than 0.01, the two consecutive diminishing-return assessments in Rounds 2 and 3 will terminate this study. Heldout outcomes have not been opened.
